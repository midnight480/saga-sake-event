import { Empty } from '@/components/ui';

/**
 * 画面を切り替えた直後に出す（Issue #60）。
 *
 * 画面の切り替えにはサーバーとの往復が 1 回入る。そのあいだ何も変わらないと、
 * スマホでは「押せていない」「遅い」と感じる。押した瞬間にこれを出す。
 * 上のヘッダとメニューはそのまま残り、下の中身だけが入れ替わる。
 * これがあると、行き先の画面の外枠を先読みしておけるので、切り替え自体も早くなる。
 */
export default function Loading() {
  return <Empty>読み込んでいます…</Empty>;
}
