# 佐賀 蔵めぐり — 設計ドキュメント

佐賀の酒蔵が集まる合同試飲イベントの、受付・在庫・チケットを 1 つにまとめた
Web サービス。**主催者 / 酒蔵 / 参加者** の 3 者が同じデータを見て動く。

- **出発点**: [claude.ai/design のプロトタイプ](https://claude.ai/design/p/5914be51-2c0f-477b-bba0-44ed7afe1035?file=%E4%BD%90%E8%B3%80+%E8%94%B5%E3%82%81%E3%81%90%E3%82%8A+%E3%82%A2%E3%83%97%E3%83%AA.dc.html)（`佐賀 蔵めぐり アプリ.dc.html`）
- **実装の前提**: Web サービス。iPhone / Android のどちらにも依存しない
- **運用の前提**: 主催者は非エンジニア。自力でデプロイして当日まで運用できること

---

## 1. なぜ Web サービスなのか

**OS に依存させないため。** ネイティブアプリにすると、

- iOS と Android の 2 本を作り、2 つの審査を通し、2 つのストアに出す必要がある
- 来場者に「ストアからダウンロードしてください」と頼むことになる（当日、会場の
  細い回線でのインストールは現実的でない）
- 蔵の担当者の端末が古い OS だと入らないことがある

一方 Web なら、**配るのは URL 1 本**で済む。会場の入口に QR を貼れば、それを
読み取った端末で即座に使える。ホーム画面に追加すればアプリのようにも見える
（`src/app/manifest.ts`）。

この判断が、以下の実装の随所に効いている:

| 決めたこと | OS 非依存のためにやっていること |
| --- | --- |
| QR 読み取り | `BarcodeDetector` は Safari に無いので使わず、両 OS で動く ZXing に一本化 |
| 入力欄の文字サイズ | 16px 未満にしない（iOS Safari が入力時に勝手にズームする） |
| 動画要素 | `playsInline` を必ず付ける（iOS が全画面再生に切り替えてしまう） |
| 画面下端の余白 | `env(safe-area-inset-bottom)` を使う（iPhone のホームバー、Android のジェスチャバー） |
| 画面の地色 | `html` にも背景色を置く（iOS のプルダウン跳ね返りで白が覗くのを防ぐ） |

---

## 2. 当日の流れ

```
【事前】
主催者  ── イベント設定（日付・時刻・蔵数）
        ── 蔵アカウントを発行 ──→ 紙で各蔵に配る
        ── チケットQRを発行 ──→ 印刷して受付に置く

【当日・開場前】
酒蔵    ── 配られたIDでログイン
        ── 持ち込んだ銘柄と本数を登録（提供できる杯数が自動で出る）
主催者  ── 「イベントを開始する」を押す ＝ 受付解禁

【当日・開催中】
参加者  ── 券のQRを読み取る ──→ チケットが入る
        ── 酒蔵をさがす（混雑と残りが見える）
        ── ブース前で銘柄を選んでリクエスト ──→ チケットが引かれる
酒蔵    ── 受付キューに並ぶ ──→ 準備中 → 準備完了 → 受渡完了
参加者  ── 状態が変わるたびに、自分の画面の文言が変わる
主催者  ── ダッシュボードで在庫・混雑・遅延を監視
        ── 券だけ売り切れたら当日券を追加発行
```

**3 者は同じ 1 つの状態を共有する。** どこか 1 台で操作すると、ほかの画面が
数秒で追いつく。

---

## 3. ドメインモデル

### 用語

| 語 | 意味 |
| --- | --- |
| **杯（cup）** | 提供の単位。四合瓶 = 6 杯、一升瓶 = 15 杯で換算する |
| **チケット（枚）** | 参加者が使う通貨。1 杯あたり 1〜3 枚を蔵が決める |
| **券** | 紙の QR 1 枚。読み取ると 10 枚分のチケットになる |
| **銘柄（item）** | 蔵が持ち込む 1 種類のお酒 |
| **リクエスト（request）** | 参加者から蔵への 1 件の注文 |

### 計算式（`src/lib/domain.ts` に集約）

```
銘柄の総杯数   = 本数 × 1本あたりの杯数
銘柄の残り杯数 = max(0, 総杯数 − 渡し終えた杯数)
蔵の残り杯数   = その蔵の全銘柄の残りの合計

対応待ち件数   = status が accepted か preparing の件数
混雑           = 0件 → 空いています / 1-2件 → やや混雑 / 3件以上 → 混雑

在庫のチケット換算 = Σ(銘柄の残り杯数 × その銘柄の1杯あたり枚数)
券切れ警告     = 当日券の残り == 0 かつ 在庫のチケット換算 > 0
応答遅延       = accepted のまま 12 分以上経過
```

**計算は必ず `domain.ts` に置く。** 画面で足し算をしない。サーバーとブラウザで
同じ式を使うことで、「主催者の画面とスタッフの画面で数が違う」を防ぐ。

### 注文の状態遷移

```
                ┌──→ preparing ──→ ready ──→ delivered ← 在庫が減るのはここだけ
   accepted ────┤         │           │
   （受付済）    │         │           │
                └─────────┴───────────┴──→ cancelled ← チケットが戻るのはここだけ
```

- **在庫が減るのは `delivered` の瞬間だけ。** 注文した時点では減らさない。
  キャンセルされた注文のために酒が消えるのはおかしいから。
- **チケットが減るのは注文した瞬間。** 戻るのは `cancelled` のとき。
  先に引き落とすことで、残高より多く頼めない。
- 遷移表は `STATUS_FLOW`。ここに無い遷移はサーバーが拒否する（`canTransition`）。

---

## 4. 整合性の設計 — ここが一番大事

イベント当日は、**同じ在庫を複数の端末が同時に触る**。「二重に減る」「二重に
戻る」が起きると、その場で誰も原因を突き止められないまま会場が混乱する。

使っている Neon の HTTP ドライバは **1 文ごとに接続が切れる**ため、
「読んで、考えて、書く」を 1 つのトランザクションにまとめられない。
そこで危険な操作は、**条件付きの 1 本の SQL** として書き、原子性を文そのものに
持たせている。0 行返ってきたら「条件を満たさなかった」＝失敗と判断する。

### 例1: 券の読み取り（`redeemTicket`）

```sql
WITH claimed AS (
  UPDATE tickets SET redeemed_by = $1, redeemed_at = now()
  WHERE code = $2 AND redeemed_by IS NULL   -- ← まだ使われていない券だけ押さえる
  RETURNING batch_id
), batch AS (...)
UPDATE guests SET tickets = tickets + (SELECT cups_per_ticket FROM batch)
WHERE clerk_user_id = $1 AND EXISTS (SELECT 1 FROM batch)
RETURNING ...
```

同じ QR を 2 台で同時に読んでも、先に届いた 1 回だけが成功する。

### 例2: 注文（`place_order` — Postgres の関数）

注文だけは 1 本の SQL に収まらなかったので、**PL/pgSQL の関数**にしている
（`src/lib/schema-sql.ts`）。CTE を並べる書き方では次の 2 つが防げなかった。

1. **同じ最後の 1 杯を 2 人が同時に取れてしまう。** CTE はすべて文の開始時点の
   スナップショットを見るため、直前に確定した他人の注文が見えない。
2. **チケットだけ引かれて注文が作られない。** データを変更する CTE は、
   主クエリがその結果を使わなくても必ず実行される。片方が条件を満たさなくても
   もう片方は取り消されない。

関数の中では順番が効く。

```
1. SELECT * FROM items WHERE id = ? FOR UPDATE     ← 銘柄の行をロック
2. 未受渡の注文の杯数を数える                        ← ロック後の文なので最新が見える
3. 残り（総杯数 − 受渡済 − 受注済）を確かめる
4. SELECT ... FROM guests ... FOR UPDATE           ← 参加者の行をロック
5. 残高を確かめて引き落とす
6. 注文を作る
```

READ COMMITTED では **文ごとに新しいスナップショット**を取るので、1 で
ロックを取ったあとの 2 は他人の確定した注文を見られる。ロックの順番は常に
**銘柄 → 参加者** に固定してデッドロックを避けている。

失敗は例外にせず `(ok, reason, request_id, spent)` の行で返す。`reason` は
そのまま画面に出せる日本語（「チケットが 2 枚 足りません。」）。

### 「残り」は 2 種類ある

上の 3 で「受注済」を引いているので、残りの意味が 2 つに分かれる。

| 関数 | 意味 | 使う場所 |
| --- | --- | --- |
| `itemCupsLeft` | **物理的な残り** = 総杯数 − 受渡済 | 主催者（会場にまだ酒があるか） |
| `itemAvailableCups` | **いま頼める数** = さらに受注済を引く | 参加者（残り / 完売の表示と注文の判定） |

参加者に物理的な残りを見せると、最後の数杯で「残り 2 杯と書いてあるのに
頼めない」が起きる。逆に主催者に頼める数だけを見せると、在庫があるのに
足りないように見える。

### 例3: 受渡完了（`setRequestStatus`）

```sql
WITH moved AS (
  UPDATE requests SET status = 'delivered'
  WHERE id = $1 AND status = 'ready'        -- ← ready からしか進めない
  RETURNING item_id, cups
)
UPDATE items SET used_cups = used_cups + (SELECT cups FROM moved) ...
```

2 人が同時に「受渡完了」を押しても、在庫は 1 回しか減らない。
負けた側には「ほかの端末が先に更新しました」と出る。

### スキーマ適用

`ensureSchema()` が初回アクセス時に 1 回だけ流す。**マイグレーション CLI は
使わない**（主催者にコマンドを打たせないため）。

全文を 1 つのトランザクションに入れ、先頭に `pg_advisory_xact_lock` を置く。
セッション版の `pg_advisory_lock` では HTTP ドライバの都合で取った直後に
解放されてしまい、排他にならない。

---

## 5. 画面一覧

### 主催者（`/organizer`）

| 画面 | 役割 |
| --- | --- |
| ダッシュボード | KPI 6 種、券切れ警告、応答遅延アラート、蔵別の在庫と混雑 |
| イベント設定 | 日付・開始/終了時刻・参加酒蔵数、開始/終了の切り替え |
| 蔵アカウント | 蔵の追加、ID の自動連番、パスワード発行・再発行、ブース割当、削除 |
| チケットQR | 券種ごとの発行数・読取数、追加発行、印刷用ページ |

補助ページ: `/organizer/breweries/print`（蔵へ配るログイン案内の台紙）、
`/organizer/tickets/print`（券の QR 台紙）

### 酒蔵（`/brewery`）

| 画面 | 役割 |
| --- | --- |
| 受付キュー | 対応待ち件数、受付の一時停止/再開、注文カードと状態を進めるボタン |
| 持ち込み登録 | 銘柄の追加（種類・精米歩合・瓶サイズ・チケット枚数）、本数の増減、合計杯数 |

### 参加者（`/guest`）

| 画面 | 役割 |
| --- | --- |
| マイページ | チケット残高、使用済、自分の注文と現在の状況の文言 |
| 酒蔵をさがす | 蔵の一覧、混雑、ブース、銘柄ごとの残り |
| 銘柄を選ぶ | 銘柄の詳細、杯数の選択、リクエスト送信 |
| チケット | QR 読み取り（カメラ）、手入力の代替、読み取り履歴 |

### 全役割共通

| 画面 | 役割 |
| --- | --- |
| `/` | ログイン済ならその人の役割へ自動で送る。未設定なら `/setup` へ |
| `/setup` | セットアップ診断。**環境変数が何も無くても必ず開く** |

---

## 6. デザイントークン

プロトタイプから取り出した値を `src/app/globals.css` の `@theme` に集約。
各画面に生の `#hex` を書かない。

| 用途 | トークン | 値 |
| --- | --- | --- |
| ページ地 | `sumi` | `#101a14` |
| 画面の地 | `surface` | `#16221b` |
| カード・入力欄 | `card` | `#1f3128` |
| くぼみ（カメラ枠） | `well` | `#0f1913` |
| 本文 | `ink` | `#f3efe4` |
| 見出しラベル・強調 | `gold` / `gold-bright` | `#c8a761` / `#e0c079` |
| 主要ボタン・危険 | `terracotta` | `#c0563f`（hover `#d06a51`、文字 `#e08a72`） |
| 注意・準備中 | `amber` | `#e8a95a` |
| 正常・完了 | `matcha` | `#8fce89` |
| 抑えたボタン | `forest` | `#3c4f44` |

書体は **Kaisei HarunoUmi**（明朝／見出しと数字）と **BIZ UDPGothic**（本文）。
`next/font` で自前配信に切り替えているので、会場の細い回線でも外部への
追加リクエストが発生しない。

寸法: カード角丸 12px、入力欄 10px、丸ボタン 100px、**触る部品は最低 44px**
（実装では 48px を既定）。

---

## 7. 技術選定と理由

| 選んだもの | 理由 | 捨てた案 |
| --- | --- | --- |
| **Next.js 16（App Router）** | Vercel に置くと設定が最小。Server Actions で API を別に作らずに済む | — |
| **Clerk** | Vercel Marketplace のネイティブ統合で、**鍵が自動で入る**（主催者が値を貼らない）。username+password のアカウントを API で作れるので、蔵アカウントの配布と再発行がそのまま実装できる | 自前のセッション実装（当日の事故が怖い） |
| **Neon Postgres** | Vercel の Storage から数クリックで追加でき、接続文字列が自動で入る。無料枠でイベント規模に足りる | SQLite（サーバーレスで持てない）、Google Sheets（同時更新に弱い） |
| **生 SQL（ORM なし）** | マイグレーション CLI の実行工程を無くすため。整合性を CTE 1 文で表現したいので、SQL を隠されたくない | Drizzle / Prisma（`migrate` コマンドが増える） |
| **4 秒ポーリング** | 追加の契約・設定が 0。会場規模（数百人）なら十分 | WebSocket / SSE（Durable Object などの追加契約が必要） |
| **ZXing（`@zxing/browser`）** | iOS Safari と Android Chrome の**両方**で同じコードが動く | `BarcodeDetector`（Safari に無く、分岐が増える） |

### ポーリングの限界について

4 秒ポーリング × 同時 300 端末 ≒ 75 req/s。Vercel の Fluid Compute なら
問題ない範囲だが、来場者が 1000 人を超える規模なら見直しが必要。
その場合の順路は「まず `/api/snapshot` に短い s-maxage を付ける」→
「役割ごとに返す量を削る」→「SSE に替える」。いまは**追加設定 0** を優先している。

---

## 8. デプロイ設計（非エンジニア前提）

**設計目標: 主催者がコマンドを 1 つも打たずに公開できること。**

そのために次の 3 つを守っている。

### (1) 環境変数が無くてもビルドと起動が成功する

これが崩れると「デプロイ → 真っ白なエラー画面」になり、主催者は自力で戻れない。

- `middleware.ts` は Clerk の鍵が無ければ `clerkMiddleware` を通さない
- `layout.tsx` は鍵が無ければ `ClerkProvider` を付けない
- DB 接続はモジュール読み込み時ではなく、実際に使う瞬間まで遅らせる
- `/setup` は DB も Clerk も使わずに描画できる

### (2) セットアップ診断が「次に押す場所」を教える

`/setup` が 4 項目を判定し、済んでいないものには**クリックする場所の名前**を
順番に並べ、Vercel の該当画面へのリンクを出す。設定を変えて再読み込みすると
判定が変わる。

### (3) スキーマ適用に人の操作が要らない

初回アクセス時に自動で流れる（§4 参照）。

### 手順の全体

```
1. README の「Deploy with Vercel」を押す
2. GitHub でログイン → リポジトリが自分のアカウントに複製される
3. デプロイ完了（ここで /setup が開く。まだ 2 項目が未設定）
4. Vercel の Storage → Neon を追加      ← クリックのみ。鍵は自動で入る
5. Vercel の Integrations → Clerk を追加 ← クリックのみ。鍵は自動で入る
6. /setup を再読み込み → 「主催者としてはじめる」
7. 運営画面へ
```

詳細は [`docs/デプロイ手順.md`](docs/デプロイ手順.md)。

### ビルドを型エラーで落とさない設定について

`next.config.ts` で `typescript.ignoreBuildErrors = true` にしている。
主催者が GitHub 上でファイルを直接編集したとき、誤字ひとつで**当日の
デプロイが止まる**方が危険だという判断。型は CI（`npm run typecheck`）で
必ず検査するので、開発時には落ちる。

---

## 9. プロトタイプから意図的に変えたこと

プロトタイプは単一ファイルのデモで、1 画面に 3 台のスマホを並べて見せる
ものだった。実装では次の点を変えている。**見た目と文言は極力そのまま**。

| 項目 | プロトタイプ | 実装 | 理由 |
| --- | --- | --- | --- |
| iOS デバイス枠（`ios-frame.jsx`） | 画面を iPhone の枠で囲う | **使わない** | モックアップを見せるための装飾。実機のブラウザでは枠の中に枠が出る。OS 非依存の方針にも反する |
| 3 役割の同時表示 | 1 画面に 3 台並べる | 役割ごとに別 URL | 実際には別の人が別の端末で使う |
| 会場時計 | 2.5 秒で 1 分進む偽の時計 | サーバーの実時刻 | — |
| ログイン | ID・パスワードの見た目だけ | Clerk の実認証 | — |
| 参加者のログイン | 参加者ID + パスワード | **メールアドレス + 届く数字** | 来場者にパスワードを作らせない。当日の「パスワードを忘れた」を無くす |
| 蔵のパスワード表示 | 一覧で何度でも表示/非表示を切替 | **発行直後の 1 回だけ表示。保存しない** | 30 蔵分の「使えるパスワード」を DB に抱えるのは危険。プロトタイプ自身が引いている NIST SP 800-63B の考え方とも矛盾する。一括印刷は「全蔵を作り直して印刷」で代替（`/organizer/breweries/print`） |
| 券の QR | 券種ごとに 1 つの絵 | **1 枚ごとに別コード**を生成し、個別に消し込む | プロトタイプが謳う「同じ券は二度使えません」を実際に守るため。コピーした紙では増えない |
| QR の中身 | — | `https://会場URL/guest/charge?code=…` | アプリの読み取り画面でも、端末標準のカメラアプリでも開ける |
| 酒蔵特別枠 | ログイン時に自由に選べる | **招待コードが必要**（`BREWERY_GUEST_INVITE_CODE`） | 誰でも 15 枚もらえてしまうため |
| 前売券の追加発行 | 不可（仕様どおり） | 不可のまま。ただし発行数 0 のときは案内を出す | 初期状態で前売券が 0 枚なので、何もできない画面に見えてしまう |

---

## 10. セキュリティ

- **権限の確認はすべてサーバー側**（`src/lib/auth.ts` の `requireOrganizer` /
  `requireBrewery`）。画面のボタンを隠すのは補助でしかない。
- **蔵は自分の蔵しか触れない**。銘柄の操作は、その銘柄の持ち主かを毎回確かめる
  （`requireItemOwner`）。主催者は当日の代理対応のため全件を触れる。
- **パスワードの生成に `Math.random` を使わない**（`node:crypto` の `randomInt`）。
  印刷して配る資格情報が予測可能では意味がない。
- **漏えい済みパスワードの弾き**は Clerk 側が行う（`skipPasswordChecks: false`)。
  プロトタイプの「漏えいパスワード一覧との照合」という記述が、そのまま満たされる。
- **蔵を削除すると Clerk のユーザーも消す**。残すと、消したはずの蔵が入れてしまう。
- 主催者の決め方は 2 通り。`ORGANIZER_EMAILS` があればそのメールの人だけ。
  無ければ**最初にログインした人**が主催者になる（初期設定を無くすため）。
  公開前に自分で 1 度ログインしておくこと。

---

## 11. 検証

### 実際の Postgres に対して検証する

```bash
npm run verify:sql   # PGHOST 等を渡して実行
```

`scripts/check-schema.mjs` が次を確かめる（CI でも毎回走る）。

- **スキーマを 2 回流して壊れないこと**（初回アクセス時に自動適用するため、
  何度流しても同じ結果になることが絶対条件）
- **本番コード中の SQL が実スキーマと合っていること。**
  `store.ts` などのテンプレートリテラルから SQL を取り出し、`PREPARE` で
  解析させる。実行はしないので副作用なしに、構文の誤り・存在しない列・
  存在しない表を捕まえられる
- **`place_order` の挙動**（残高不足・受付停止・在庫超過・存在しない銘柄）
- **同時注文で売り過ぎないこと。** 12 人が最後の 1 杯を同時に取ろうとして、
  通るのが 1 件だけであること
- **同じ券を同時に 8 回読んでも 1 回しか加算されないこと**
- **受渡で在庫が減り、キャンセルでチケットが戻り、二重には動かないこと**

### 環境変数なしで動くこと

CI が `DATABASE_URL` も Clerk の鍵も空にしてビルドを通す。
§8 の前提が崩れていないかを機械的に守るため。

### 検証で見つかった実際の不具合

記録として残す。どちらも「実際に動かさないと分からない」種類のものだった。

| 見つかったもの | 原因 | 直し方 |
| --- | --- | --- |
| **開催日が 1 日前になる** | `date` 型はローカル 0 時の `Date` として返る。それを `toISOString()` すると日本時間では 9 時間戻り、前日になる | ローカルの年月日を組み立てる（`toDateString`） |
| **スキーマ適用の排他が効いていない** | Neon の HTTP ドライバは 1 文ごとに接続を閉じるので、セッション版 `pg_advisory_lock` は取った直後に解放される | 1 トランザクションにまとめ、`pg_advisory_xact_lock` に変更 |
| **最後の数杯が売り過ぎになる** | CTE はすべて文の開始時点のスナップショットを見るため、直前に確定した他人の注文が見えない | PL/pgSQL 関数に移し、`FOR UPDATE` のあとで数え直す |

---

## 12. 既知の制約・これから

- **前売券の QR を外部の販売サイトで発行した場合の受け渡し**が未実装。
  いまは主催者がこのアプリで発行した券だけが使える。
- **会場マップ**が無い。ブース番号は出るが図がない。
- **主催者の大画面運用**は、いまはスマホ幅のままで中央に表示される。
  PC で複数の蔵を横並びで監視する画面は未実装。
- **蔵の据置端末向けの表示**（大きな文字で注文だけを出すモード）が未実装。
- **主催者が代理で蔵を操作するとき**、いまは 1 蔵目に固定。蔵を選ぶ UI が必要。
- **オフライン対応**が無い。プロトタイプは「回線が切れている間の読み取りは端末に
  残り、復旧時にまとめて反映」と謳っているが、二重消し込みの危険があるため
  実装していない。通信が切れている間は前回の値を出し続け、警告帯を表示する。
