# 佐賀 蔵めぐり — 設計ドキュメント

佐賀の酒蔵が集まる合同試飲イベントの、受付・在庫・ポイントを 1 つにまとめた
Web サービス。**主催者 / 酒蔵 / 参加者** の 3 者が同じデータを見て動く。

- **出発点**: [claude.ai/design のプロトタイプ](https://claude.ai/design/p/5914be51-2c0f-477b-bba0-44ed7afe1035?file=%E4%BD%90%E8%B3%80+%E8%94%B5%E3%82%81%E3%81%90%E3%82%8A+%E3%82%A2%E3%83%97%E3%83%AA.dc.html)（`佐賀 蔵めぐり アプリ.dc.html`）
- **実装の前提**: Web サービス。iPhone / Android のどちらにも依存しない
- **運用の前提**: 主催者は非エンジニア。自力でデプロイして当日まで運用できること
- **運用の前提**: **3 者ともスマホを持って会場を歩き回る**。PC を開けない場面がある

---

## 1. なぜ Web サービスなのか

**OS に依存させないため。** ネイティブアプリにすると 2 本作って 2 つの審査を通す
必要があり、来場者にもインストールを頼むことになる。会場の細い回線でそれは
現実的でない。Web なら**配るのは URL 1 本**で済む。

この判断が実装の随所に効いている。

| 決めたこと | OS 非依存のためにやっていること |
| --- | --- |
| QR 読み取り | `BarcodeDetector` は Safari に無いので使わず、両 OS で動く ZXing に一本化 |
| 入力欄の文字サイズ | 16px 未満にしない（iOS Safari が入力時に勝手にズームする） |
| 動画要素 | `playsInline` を必ず付ける（iOS が全画面再生に切り替える） |
| 画面下端の余白 | `env(safe-area-inset-bottom)` を使う |
| 画面の地色 | `html` にも背景色を置く（iOS のプルダウン跳ね返り対策） |
| 通知 | iOS はホーム画面に追加しないと受け取れない。だから追加をすすめる案内を出す |

### 画面幅について

**スマホ 1 台で完結する作りにしている。** 幅は 480px までに抑えて中央に置き、
大きな画面でも横に広げない。

主催者も会場を歩き回るので、3 者とも同じ見え方になる。これは利点でもあり、
当日「その画面の、下から 2 番目です」と口頭で案内が通る。

実測では 320px から 1440px まで横スクロールもはみ出しも無い。

---

## 2. 当日の流れ

```
【事前】
主催者  ── イベント設定（日付・時刻・蔵数・券種ごとのポイント）
        ── 蔵アカウントを発行 ──→ 紙で各蔵に配る
        ── 券（1 枚ごとに QR 付き）を必要な枚数 刷る ──→ 受付に置く

【当日・開場前】
酒蔵    ── 配られた蔵IDでログイン
        ── 持ち込んだ銘柄・1杯あたりのポイント・本数を登録

【当日】
                開始時刻になると自動で受付が開く
参加者  ── 受付で券を受け取る
        ── 券の QR をカメラで読む ──→ ポイントが入る
        ── 酒蔵をさがす（混雑・残り・必要ポイントが見える）
        ── 銘柄を選んでリクエスト ──→ ポイントが引かれ、マイページへ移る
酒蔵    ── 受付キューで 準備中 → 準備完了 → 受渡完了
参加者  ── 状態が変わるたびに、自分の画面の文言が変わる
主催者  ── ダッシュボードで在庫・混雑・遅延を監視
                終了時刻になると自動で受付が閉じる

【次の年】
主催者  ── イベント設定から「前回の記録を片付ける」
```

**3 者は同じ 1 つの状態を共有する。** どこか 1 台で操作すると、ほかの画面が
数秒で追いつく。

---

## 3. ドメインモデル

### 用語

| 語 | 意味 |
| --- | --- |
| **杯（cup）** | 提供の単位。四合瓶 = 6 杯、一升瓶 = 15 杯で換算する |
| **ポイント** | 参加者が持つ残高。1 杯あたり 1〜20 ポイントを蔵が決める |
| **券** | 紙 1 枚。コードが書かれており、読み取ると決められたポイントが入る |
| **銘柄（item）** | 蔵が持ち込む 1 種類のお酒 |
| **リクエスト（request）** | 参加者から蔵への 1 件の注文 |

> **単位の使い分け** — 紙の券だけが「枚」。参加者の残高と銘柄の値段は
> 「ポイント」。以前はどちらも「枚」で、券 1 枚 → チケット 10 枚 → 1 杯 1 枚 と
> 3 つの意味が混ざって読めなくなっていた。

### 計算式（`src/lib/domain.ts` に集約）

```
銘柄の総杯数     = 本数 × 1本あたりの杯数
物理的な残り     = max(0, 総杯数 − 渡し終えた杯数)        … 主催者が見る在庫
いま頼める杯数   = max(0, 総杯数 − 渡し終えた − 受注済み)  … 参加者が見る残り

対応待ち件数     = status が accepted か preparing の件数
混雑             = 0件 → 空いています / 1-2件 → やや混雑 / 3件以上 → 混雑

在庫のポイント換算 = Σ(物理的な残り × その銘柄の1杯あたりポイント)
券切れ警告       = 当日券の残り == 0 かつ 在庫のポイント換算 > 0
応答遅延         = accepted のまま 12 分以上経過
```

**計算は必ず `domain.ts` に置く。** 画面で足し算をしない。

### 「残り」が 2 種類ある理由

参加者に物理的な残りを見せると、最後の数杯で「残り 2 杯と書いてあるのに
頼めない」が起きる。逆に主催者に頼める数だけを見せると、在庫があるのに
足りないように見える。見る人によって必要な数が違う。

### 注文の状態遷移

```
                ┌──→ preparing ──→ ready ──→ delivered ← 在庫が減るのはここだけ
   accepted ────┤         │           │
   （受付済）    │         │           │
                └─────────┴───────────┴──→ cancelled ← ポイントが戻るのはここだけ
```

- **在庫が減るのは `delivered` の瞬間だけ。** 注文の時点では減らさない
- **ポイントが減るのは注文した瞬間。** 戻るのは `cancelled` のとき
- 遷移表は `STATUS_FLOW`。ここに無い遷移はサーバーが拒否する

### 受付の開け閉め

```
auto     予定どおり（既定）。開始時刻〜終了時刻のあいだだけ受け付ける
open     主催者が手で開けている
closed   主催者が手で閉じている
```

**原則は予定どおり。** 手動は例外なので、開くときも閉じるときも確認を挟む。
上書き中は「予定どおりにもどす」で戻せる。

> **時刻は必ず日本時間で組み立てる。** `now.getHours()` を使ってはいけない。
> Vercel のサーバーは UTC で動くので 9 時間ずれ、11:00 開始が 02:00 に開く。
> `scheduleWindow()` が `+09:00` を付けて絶対時刻に直している。

---

## 4. 整合性の設計 — ここが一番大事

イベント当日は、**同じ在庫を複数の端末が同時に触る**。「二重に減る」「二重に
戻る」が起きると、その場で誰も原因を突き止められないまま会場が混乱する。

Neon の HTTP ドライバは **1 文ごとに接続が切れる**ため、「読んで、考えて、書く」
を 1 つのトランザクションにまとめられない。そこで危険な操作は、**条件付きの
1 本の SQL**（CTE 付き UPDATE）として書き、原子性を文そのものに持たせている。
0 行返ってきたら「条件を満たさなかった」＝失敗と判断する。

### 例1: 券の読み取り（`redeemTicket`）

```sql
WITH claimed AS (
  UPDATE tickets SET redeemed_by = $1, redeemed_at = now()
  WHERE code = $2 AND redeemed_by IS NULL   -- ← まだ使われていない券だけ押さえる
  RETURNING batch_id
), batch AS (...)
UPDATE guests SET tickets = tickets + (SELECT cups_per_ticket FROM batch)
WHERE clerk_user_id = $1 AND EXISTS (SELECT 1 FROM batch)
```

同じコードを 2 台で同時に入れても、先に届いた 1 回だけが成功する。

### 例2: 注文（`place_order` — Postgres の関数）

注文だけは 1 本の SQL に収まらなかったので、**PL/pgSQL の関数**にしている。
CTE を並べる書き方では次の 2 つが防げなかった。

1. **同じ最後の 1 杯を 2 人が同時に取れてしまう。** CTE はすべて文の開始時点の
   スナップショットを見るため、直前に確定した他人の注文が見えない
2. **ポイントだけ引かれて注文が作られない。** データを変更する CTE は、主クエリが
   その結果を使わなくても必ず実行される

関数の中では順番が効く。

```
1. SELECT * FROM items WHERE id = ? FOR UPDATE     ← 銘柄の行をロック
2. 未受渡の注文の杯数を数える                        ← ロック後の文なので最新が見える
3. 残り（総杯数 − 受渡済 − 受注済）を確かめる
4. SELECT ... FROM guests ... FOR UPDATE           ← 参加者の行をロック
5. まだ受け取っていない注文が無いか確かめる          ← Issue #34
6. 残高を確かめて引き落とす
7. 注文を作る
```

5 は **参加者の行をロックしたあと**に置くのが要。同じ人が 2 台の端末から同時に
押しても、2 本目はロックが空くのを待ち、そのあとの文で 1 本目の注文が見えるので
ここで止まる。「未受取」は受付済・準備中・**準備完了**の 3 つ（`UNDELIVERED_STATUSES`）。
準備完了は蔵の手を離れているが、参加者の手元にはまだ無い。

この制約を入れたのは、受け取りに来ないまま次々と頼まれると、蔵の前に杯が並んで
取り違えが起きるため。一度に 3 杯まで（`MAX_CUPS_PER_REQUEST`）と合わせて、
1 人が同時に抱えられるのは最大 3 杯になる。

READ COMMITTED では **文ごとに新しいスナップショット**を取るので、1 でロックを
取ったあとの 2 は他人の確定した注文を見られる。ロックの順番は常に
**銘柄 → 参加者** に固定してデッドロックを避けている。

### 例3: 受渡完了（`setRequestStatus`）

```sql
WITH moved AS (
  UPDATE requests SET status = 'delivered'
  WHERE id = $1 AND status = 'ready'        -- ← ready からしか進めない
  RETURNING item_id, cups
)
UPDATE items SET used_cups = used_cups + (SELECT cups FROM moved) ...
```

2 人が同時に押しても、在庫は 1 回しか減らない。

### 例4: 節目のお知らせ（`sendDueNotices`）

送った記録を**先に作り、作れたときだけ送る**。開催日と節目の組を主キーに
しているので、何台が同時に来ても 1 台しか記録を作れない ＝ 二重に送らない。

### スキーマ適用

`ensureSchema()` が初回アクセス時に 1 回だけ流す。**マイグレーション CLI は
使わない**（主催者にコマンドを打たせないため）。

全文を 1 つのトランザクションに入れ、先頭に `pg_advisory_xact_lock` を置く。
セッション版の `pg_advisory_lock` では HTTP ドライバの都合で取った直後に
解放されてしまい、排他にならない。

列や制約を変えるときも、この中に「何度流しても同じ結果になる」文として足す
（`ALTER TABLE ... DROP CONSTRAINT IF EXISTS` → `ADD CONSTRAINT`）。

---

## 5. 認証と役割

3 役割とも Clerk を使うが、**入口が 2 つある**。

| 役割 | ログイン方法 | 入口 |
| --- | --- | --- |
| 主催者 | メールアドレス + 届く数字 | `/sign-in` |
| 参加者 | メールアドレス + 届く数字 | `/sign-up` |
| **酒蔵** | **蔵ID + パスワード** | **`/brewery-login`** |

### 酒蔵だけ専用の入口を持つ理由

蔵アカウントには**受信できないメールアドレス**を割り当てている（`kura-001@kura.…`）。
蔵の担当者は当日交代することもあり、個人のアドレスを集めて回るのは現実的でない。

ところが Clerk の標準ログイン画面で入ろうとすると、**「新しいデバイスからの
サインイン」を検知して、そのアドレス宛に確認コードを送ってしまう**。届かないので
蔵は永久に入れない。

そこで `/brewery-login` では次の流れにしている。

```
蔵IDとパスワードを入れる
  → サーバーが Clerk の管理 API でパスワードを照合（users.verifyPassword）
  → 通ったらサインイン用の引換券を発行（signInTokens.createSignInToken、有効 2 分）
  → その引換券で Clerk がセッションを張る
```

対話的な確認が一切入らないので、メールが届かなくても入れる。照合も発行も
Clerk の管理 API なので、**認証の土台は Clerk のまま**である。

### 役割の判定は DB を正とする

セッションのトークン（`sessionClaims.publicMetadata`）から読んではいけない。
**Clerk は既定で publicMetadata をトークンに含めない**ので常に空になり、蔵の
担当者が参加者として扱われる（実際に起きた）。

`breweries.clerk_user_id` に索引を張り、ログインしている人が担当する蔵を
一文で引く。この問い合わせは `store.ts` ではなく `auth.ts` に直接書いている。
`auth.ts` はミドルウェア（Edge ランタイム）からも読み込まれるが、`store.ts` は
`node:crypto` を使うので取り込むとビルドが壊れるため。

### 主催者の決め方

`ORGANIZER_EMAILS` があればそのメールの人だけ。無ければ**最初にログインした人**が
主催者になる（初期設定を無くすため）。

Clerk を開発用から本番用に切り替えると**内部の ID が変わる**ので、
`ORGANIZER_EMAILS` を設定していないと主催者権限が外れる。デプロイ時に入れて
もらう案内にしているのはこのため。

---

## 6. 券とポイントの流れ

```
主催者が券種ごとにポイントを決める（イベント設定）
        ↓
必要な枚数を刷る（チケットQR）
        ↓
券は 1 人 1 枚・一度きり。1 枚ごとに、そのコード入りの QR が付く
        ↓
参加者が券の QR をカメラで読む（/guest/charge?code=… が開く）
        ↓
ポイントが入り、そのコードは使えなくなる
```

**券 1 枚ごとに QR を付ける**（Issue #33）。いちど「受付に共通の QR を 1 枚だけ
貼り、券にはコードの文字だけを刷る」形にしたが、共通 QR にはコードが入って
いないため、参加者がアプリのカメラで読んでも何も起きなかった。券はどのみち
1 枚ずつ切り離して渡すので、そこに QR を足しても手間は増えない。

QR の下にはコードの文字も残す。カメラが使えない端末でも打ち込めるように。

ログイン前に券を読んだ人は、ログインのあと同じ URL（`?code=` 付き）に戻す。
戻さないとコードが失われ、もう一度読むことになる。レイアウトからは今の URL が
見えないので、ミドルウェアが `x-request-path` ヘッダで渡している。

発行したコードは主催者の画面に一覧で出る。使い終わったものは取り消し線が付くので、
当日「このコードが使えない」と言われたときにその場で確かめられる。

---

## 7. 画面一覧

### 主催者（`/organizer`）

| タブ | 役割 |
| --- | --- |
| 運営状況 | KPI 6 種、券切れ警告、応答遅延アラート、蔵別の在庫と混雑（受付停止中は黄色） |
| 設定 | 日付・時刻・参加酒蔵数、券種ごとのポイント、受付の開け閉め、前回の記録の片付け |
| 酒蔵 | 蔵の追加、蔵IDの自動連番、パスワード発行・再発行、ログイン状態の表示、削除 |
| チケット | 券種ごとの発行枚数、コード一覧、印刷（1 枚ごとに QR）、取り消し |
| 問合せ | 蔵・参加者からの問い合わせと返信（未回答の件数をタブに表示） |
| ヘルプ | 主催者向けの使い方 |

補助ページ: `/organizer/breweries/print`（蔵へ配るログイン案内）、
`/organizer/tickets/print`（切り離して渡す券。1 枚ごとに QR とコード）

### 酒蔵（`/brewery`）

| タブ | 役割 |
| --- | --- |
| 受付キュー | 対応待ち件数、受付の一時停止/再開、注文カードと状態を進めるボタン |
| 持ち込み登録 | 銘柄の追加、1杯あたりのポイント、本数の増減、合計杯数、味わいの型と説明（任意） |

**味わいの型と説明（Issue #40）。** 初めての人には、銘柄名だけではどんなお酒か
分からない。蔵が任意で添えられるようにし、参加者が銘柄を選ぶ画面に出す。

- 型は **濃淡（淡麗 / 濃醇）** と **甘辛（大甘口 / 甘口 / 普通 / 辛口 / 大辛口）** を
  1 つずつ。説明を読まなくても一目で分かるように、札（チップ）で出す
- 説明は 200 文字まで。日本語 1 文字・絵文字 1 つを 1 文字と数える。画面の
  `countChars`（`Array.from`）と DB の `char_length` は同じ数え方になる
- 選べる値と長さは DB の CHECK でも縛る。画面や操作をすり抜けても、候補外の
  値は入らない
- 登録したあとでも直せる。誤字のために銘柄を消して作り直すと、本数や受けた
  注文まで消えてしまうため
- 既存の表には `ADD COLUMN IF NOT EXISTS` で列を足す。今の `main` の定義で作った
  表に流し、既存の銘柄が残ったまま空の列が足されることを確かめた
| ヘルプ | 酒蔵向けの使い方＋主催者への問い合わせ |

### 参加者（`/guest`）

| タブ | 役割 |
| --- | --- |
| マイページ | ポイント残高、自分の注文と現在の状況の文言 |
| 酒蔵をさがす | 蔵の一覧、混雑、銘柄ごとの必要ポイントと残り、行ける蔵の数 |
| チケット | 券のコード入力（券の QR をカメラで読めばここが開いて加算される）、読み取り履歴 |
| 記録 | 蔵・銘柄ごとの制覇（受渡完了だけを数える）、注文の履歴を時刻つきで新しい順に |
| ヘルプ | 参加者向けの使い方＋主催者への問い合わせ |

「記録」はマイページと役割を分けている。マイページは「いま取りに行くべきもの」を
見る画面、記録は振り返る画面。制覇に数えるのは受渡完了だけで、頼んだだけ・
準備中・キャンセルは含めない（Issue #30, #32）。

### メニューは上に固定する（全役割、Issue #36）

ヘッダとメニューを画面の上に置き、`sticky` で留める。以前はメニューを下に
置いていたが、外枠が `min-h-dvh`（最低でも画面の高さ）なのでページごと伸び、
メニューは中身のいちばん最後に押し出されていた。蔵が増えたり注文が積み上がる
ほど、メニューに届くまで長くスクロールすることになる。

- 通信が不安定なときの帯も、固定した部分に入れる。どこまでスクロールしても見える
- `html` に `scroll-padding-top` を付け、入力欄を押したときや場所へ飛んだときに、
  固定した部分の下へ潜り込まないようにする
- 画面下端は `main` の下の余白に `env(safe-area-inset-bottom)` を足し、最後の行が
  ホームバーに隠れないようにする

### ログアウト（全役割、Issue #31）

メニューのいちばん右に置き、押すと必ず確認を挟む。確認の文面は役割ごとに変える。
**消えないもの**と、**入り直すのに何が要るか**を先に伝えるため。

| 役割 | 戻り先 | 確認で伝えること |
| --- | --- | --- |
| 主催者 | `/` | イベントのデータは消えない。同じメールで続きから |
| 酒蔵 | `/brewery-login` | **入り直すには蔵ID とパスワードが要る。** 控えが無ければ主催者に再発行を |
| 参加者 | `/` | ポイントと記録は消えない。同じメールで続きから |

蔵のパスワードは発行直後の 1 回しか見られないので、控えを失くした蔵が
うっかりログアウトすると当日入れなくなる。確認の画面でいちばん強く書いている。

主催者のメニューはこれで 7 つになる。幅 320px でもはみ出さず、いちばん狭い
項目で 45px（指で押せる 44px 以上）あることを実測した。メニューの各項目には
`min-w-0` を付けている。無いと flex の子は中身より縮まず、長い名前が 1 つ
あるだけでメニューごと右にはみ出す。

### 全役割共通

| 画面 | 役割 |
| --- | --- |
| `/` | ログイン済ならその人の役割へ自動で送る。未設定なら `/setup` へ |
| `/setup` | セットアップ診断。**環境変数が何も無くても必ず開く** |
| `/brewery-login` | 酒蔵専用のログイン |

---

## 8. デザイントークン

プロトタイプから取り出した値を `src/app/globals.css` の `@theme` に集約。
各画面に生の `#hex` を書かない。

| 用途 | トークン | 値 |
| --- | --- | --- |
| ページ地 | `sumi` | `#101a14` |
| 画面の地 | `surface` | `#16221b` |
| カード・入力欄 | `card` | `#1f3128` |
| くぼみ（カメラ枠） | `well` | `#0f1913` |
| 本文 | `ink` | `#f3efe4` |
| 見出しラベル・強調 | `gold` / `gold-bright` | `#c8a761` / `#e0c079` |
| 主要ボタン・危険 | `terracotta` | `#c0563f`（hover `#d06a51`、文字 `#e08a72`） |
| 注意・受付停止 | `amber` | `#e8a95a` |
| 正常・完了 | `matcha` | `#8fce89` |
| 抑えたボタン | `forest` | `#3c4f44` |

書体は **Kaisei HarunoUmi**（明朝／見出しと数字）と **BIZ UDPGothic**（本文）。
`next/font` で自前配信に切り替えているので、会場の細い回線でも外部への
追加リクエストが発生しない。

OGP 画像と favicon もこの配色で作っている。書体は**使う文字だけを切り出して**
同梱（3.8 MB → 19.6 KB）。favicon は寸法で絵を変えており、16px は画数の多い
「蔵」が潰れるので徳利の形にしている。

寸法: カード角丸 12px、入力欄 10px、丸ボタン 100px、**触る部品は最低 44px**
（実装では 48px を既定）。

---

## 9. 技術選定と理由

| 選んだもの | 理由 | 捨てた案 |
| --- | --- | --- |
| **Next.js 16（App Router）** | Vercel に置くと設定が最小。Server Actions で API を別に作らずに済む | — |
| **Clerk** | Vercel Marketplace のネイティブ統合で**鍵が自動で入る**。管理 API でパスワード照合とサインイン引換券が使える | 自前のセッション実装 |
| **Neon Postgres** | Vercel の Storage から数クリックで追加でき、接続文字列が自動で入る | SQLite、Google Sheets |
| **生 SQL（ORM なし）** | マイグレーション CLI の実行工程を無くすため。整合性を 1 文で表現したい | Drizzle / Prisma |
| **4 秒ポーリング** | 追加の契約・設定が 0。節目のお知らせもこの機会に送れる | WebSocket / SSE |
| **ZXing（`@zxing/browser`）** | iOS Safari と Android Chrome の**両方**で同じコードが動く | `BarcodeDetector` |
| **Web Push（`web-push`）** | 会場を歩き回る人に節目を伝えられる。VAPID 鍵は自動生成して DB に置く | 外部の通知サービス |

### 設定を増やさないための工夫

主催者に触らせる環境変数は `ORGANIZER_EMAILS` **1 つだけ**。ほかはすべて
自動で入るか、自動で作られる。

- **DB と Clerk の鍵** … Vercel の連携が入れる（接頭辞つきでも読める。後述）
- **VAPID 鍵** … 最初に必要になった時点で自動生成し、DB に保存
- **スキーマ** … 初回アクセス時に自動適用

### 環境変数の名前ゆれ

Vercel Marketplace から入れると、変数名に製品カテゴリ名の接頭辞が付く。

```
Neon  → STORAGE_POSTGRES_URL, STORAGE_DATABASE_URL_UNPOOLED, ...
Clerk → AUTHENTICATION_CLERK_SECRET_KEY
        NEXT_PUBLIC_AUTHENTICATION_CLERK_PUBLISHABLE_KEY
```

`src/lib/env.ts` が、決め打ちの候補 → 末尾一致 の順で探す。接頭辞を自分で
変えた人でも動く。空文字は「設定されていない」として扱う（`.env.example` が
あると Vercel が同名の空の変数を作るため、素の名前だけ見ると誤判定する）。

Clerk への渡し方には落とし穴が 2 つある。

- 公開鍵は `NEXT_PUBLIC_` で始まる名前でないと、Next.js がブラウザ側のコードに
  値を埋め込まない。サーバーで読んだ値を `ClerkProvider` に props で明示的に渡す
- `clerkMiddleware` に `secretKey` を渡すと `CLERK_ENCRYPTION_KEY` が別途必須になる。
  渡さずに、Clerk が `process.env` を読むより先に名前をそろえる
  （`src/lib/env-init.ts` を `@clerk` より前に import）

### お知らせの配信に cron を使わない理由

Vercel の無料プランでは **cron が 1 日 1 回まで**で、分単位の節目に使えない。
画面が数秒ごとに現在値を取りに来るので、そのついでに節目を過ぎたかを見て送る。

誰も開いていない時間帯のぶんを後からまとめて鳴らしても意味が無いので、
**過ぎてから 20 分を超えた節目は送らない**。

### 準備完了を本人に知らせる（Issue #35）

画面の文言は 4 秒ごとに変わるが、スマホをしまっていれば気づけない。蔵の前に
杯が置かれたままになると、取り違えや温度の変化が起きる。蔵が「準備完了」に
進めたら、**頼んだ本人の端末にだけ** OS の通知を送る。

- 送るのは、状態を「準備完了」に進めるのに**成功したとき**だけ。更新は
  「いまが準備中なら」を条件にした 1 文なので、同じ注文で 2 回成功することは
  なく、二重には届かない
- 送信は `after()` で応答のあとに回す。通知の配信サービスが遅くても、蔵の
  画面を待たせない
- 送る直前にもう一度「準備完了のままか」を確かめる。その間に取り消されて
  いたら送らない
- 通知には注文ごとの `tag` を付ける。別の注文の知らせを上書きしない
- 通知を押すと、開いているタブを前に出したうえで**マイページへ移す**。
  前に出すだけでは、別の画面を開いていた人には何のことか分からない

通知の設定はヘルプにもあるが、そこまで探しに行く人はいない。受け取りを
待っている間だけ、マイページに「通知を受け取る」を出す。断られた端末や
使えない端末では何も出さない。

### お知らせの履歴と未読・既読（右上の 🔔）

OS の通知は一度消すと見返せない。iPhone はホーム画面に追加しないと届きもせず、
通知を許可していない人もいる。**届いたかどうかに関係なく、アプリの中に残す。**

- 表は `notices`（お知らせ）と `notice_reads`（誰が読んだか）の 2 つ。
  `notices.clerk_user_id` が NULL のものは全員あて（開始 10 分前などの節目）
- 読んだかどうかは人ごとに 1 行。全員あての 1 件でも、読んだ人と読んでいない
  人がいるので、お知らせの側には持たせない
- 未読の件数は 4 秒ごとの現在値（`/api/snapshot`）に入れる。新しいお知らせは
  すぐに 🔔 にしるしが付く。数えるのは 1 本の SQL
- 既読にするのは `INSERT … SELECT … ON CONFLICT DO NOTHING` の 1 文。何度押しても、
  2 台から同時に押しても 1 行のまま。**見えるお知らせ（自分あて・全員あて）の
  ときだけ**記録を作るので、ほかの人あての id を渡されても何も起きない
- 記録は送信より先に作る。準備完了なら状態を進めるのに成功した 1 回、節目なら
  `sent_notices` の記録を作れた 1 台だけが作るので、履歴も二重にならない
- 押すと既読にして、そのお知らせの画面（準備完了ならマイページ）へ移る
- 「前回の記録を片付ける」で履歴も消える。読んだ記録は外部キーで一緒に消える

🔔 は見出しの右端、受付状態の表示の隣に置く（44×44px）。その幅のぶん、見出し横の
英字の役割名は、蔵名や参加者番号があるときは省いた（§11）。幅 375px 以上なら、
受付を手動で止めている間でも蔵名・参加者番号は全部見える。320〜360px の小さい
端末で手動停止中のときだけは「…」で省略される。

### ポーリングの限界について

4 秒ポーリング × 同時 300 端末 ≒ 75 req/s。Vercel の Fluid Compute なら
問題ない範囲だが、来場者が 1000 人を超える規模なら見直しが必要。

---

## 10. デプロイ設計（非エンジニア前提）

**設計目標: 主催者がコマンドを 1 つも打たずに公開できること。**

### (1) 環境変数が無くてもビルドと起動が成功する

これが崩れると「デプロイ → 真っ白なエラー画面」になり、主催者は自力で戻れない。

- `middleware.ts` は Clerk の鍵が無ければ `clerkMiddleware` を通さない
- `layout.tsx` は鍵が無ければ `ClerkProvider` を付けない
- DB 接続はモジュール読み込み時ではなく、実際に使う瞬間まで遅らせる
- `/setup` は DB も Clerk も使わずに描画できる

### (2) セットアップ診断が「次に押す場所」を教える

`/setup` が 4 項目を判定し、済んでいないものには**クリックする場所の名前**を
順番に並べる。**いまどの環境変数を使っているか**も出すので、名前ゆれで
詰まったときに自分で切り分けられる。

### (3) スキーマ適用に人の操作が要らない

初回アクセス時に自動で流れる（§4 参照）。

### 手順の全体

```
1. README の「Deploy with Vercel」を押す（配布を受けた人）
   リポジトリを自分で持っているなら vercel.com/new から Import
2. Repository Name に自分のイベント名を入れる（これがそのまま URL になる）
3. ORGANIZER_EMAILS に自分のメールアドレスを入れる
4. Deploy
5. Vercel の Settings → Deployment Protection で Vercel Authentication を切る
   ← これを忘れると、URL を配っても来場者は全員ログイン画面で止まる
6. 左メニューの Storage → Neon を追加
7. 左メニューの Integrations → Clerk を追加
8. /setup を再読み込み →「主催者としてはじめる」
```

来場者が 100 人を超えるなら、**独自ドメインを用意して Clerk を本番モードに
切り替える**必要がある（開発モードは合計 100 人まで、あとから引き継げない）。
手順は [`docs/独自ドメインで本番運用する.md`](docs/独自ドメインで本番運用する.md)。

詳細は [`docs/デプロイ手順.md`](docs/デプロイ手順.md)。

### ビルドを型エラーで落とさない設定について

`next.config.ts` で `typescript.ignoreBuildErrors = true` にしている。
主催者が GitHub 上でファイルを直接編集したとき、誤字ひとつで**当日の
デプロイが止まる**方が危険だという判断。型は CI（`npm run typecheck`）で
必ず検査する。

---

## 11. プロトタイプから意図的に変えたこと

プロトタイプは単一ファイルのデモで、1 画面に 3 台のスマホを並べて見せる
ものだった。**見た目と文言は極力そのまま**に、次の点を変えている。

| 項目 | プロトタイプ | 実装 | 理由 |
| --- | --- | --- | --- |
| iOS デバイス枠（`ios-frame.jsx`） | 画面を iPhone の枠で囲う | **使わない** | モックアップの装飾。実機では枠の中に枠が出る |
| 3 役割の同時表示 | 1 画面に 3 台並べる | 役割ごとに別 URL | 実際には別の人が別の端末で使う |
| 会場時計 | 2.5 秒で 1 分進む偽の時計 | サーバーの実時刻（日本時間で解釈） | — |
| 受付の開け閉め | 完全に手動 | **時刻で自動**。手動は例外で確認を挟む | 時刻を入力させておいて使わないのはおかしい |
| 参加者のログイン | 参加者ID + パスワード | **メールアドレス + 届く数字** | 来場者にパスワードを作らせない |
| 参加区分の選択 | ログイン時に自由に選べる | **廃止** | 選ぶだけでポイントが配られ、受付を通さず飲めてしまった |
| 蔵のパスワード表示 | 一覧で何度でも表示 | **発行直後の 1 回だけ。保存しない** | 30 蔵分の使えるパスワードを抱えない |
| 蔵のログイン | Clerk 標準画面 | **専用画面＋引換券**（§5） | 届かないメールに確認コードが飛ぶため |
| 券の QR | 券種ごとに 1 つの絵 | **券 1 枚ごとに、そのコード入りの QR** | 共通の QR にはコードが入らず、読んでも何も起きなかった（#33） |
| 1 杯あたりのポイント | 1〜3 の 3 択 | **1〜20 を蔵が自由に** | 値付けは蔵が決めるもの |
| 前売券の追加発行 | 不可 | **可能に** | 販売数は主催者が決めるもの |
| 所在地・ブース | 14 市町から選択・自動割当 | **どちらも廃止** | 30 蔵の登録で手間になるだけだった |
| 単位 | すべて「枚」 | **券は「枚」、残高と値段は「ポイント」** | 3 つの意味が混ざって読めなかった |
| 見出し横の表記 | `BREWERY ─ 蔵名` | **蔵名・参加者番号があれば英字を省く** | 右上に 🔔 を置いたら、iPhone 15 Pro でも蔵名が「…」で切れた |

---

## 12. セキュリティ

- **権限の確認はすべてサーバー側**（`requireOrganizer` / `requireBrewery`）。
  画面のボタンを隠すのは補助でしかない
- **蔵は自分の蔵しか触れない**。銘柄の操作は持ち主かを毎回確かめる
- **パスワードの生成に `Math.random` を使わない**（`node:crypto` の `randomInt`）
- **漏えい済みパスワードの弾き**は Clerk 側が行う（`skipPasswordChecks: false`）
- **蔵を削除すると Clerk のユーザーも消す**
- **蔵IDが実在しなくてもログイン失敗の文言を変えない**（総当たりで探らせない）
- **サインイン引換券の有効期限は 2 分**
- **ポイントが入る道は券のコードだけ**。受付を通さず得る経路が無い
- **見せてよいものだけを送る。画面で隠すのではなく、送る前に絞る**（Issue #39）。
  会場の現在値（`/api/snapshot`）は、以前は誰にでも全員分を返し、画面の側で
  自分の分だけを表示していた。通信の中身には全部入っていたので、ログインして
  いない人でも、全員の注文と蔵のログイン ID を取り出せた

  | 見ている人 | 注文 | 蔵のログイン ID | 券種・問い合わせ件数 |
  | --- | --- | --- | --- |
  | ログインしていない | **何も返さない（401）** | ― | ― |
  | 主催者 | 全員分 | 返す | 返す |
  | 酒蔵 | 自分の蔵の分（参加者の内部 ID は外す） | 返さない | 返さない |
  | 参加者 | 自分の分 | 返さない | 返さない |

  参加者の一覧の混み具合（待ち件数）は、蔵ごとの**件数だけ**をサーバーで数えて
  返す（`waitingByBrewery`）。誰の注文かは含まない。参加者の記録は、本人が見て、
  SNS に載せるかどうかを本人が決めるもの

---

## 13. 検証

### 実際の Postgres に対して

```bash
npm run verify:sql   # PGHOST 等を渡して実行
```

`scripts/check-schema.mjs` が次を確かめる（CI でも毎回走る）。

- **スキーマを 2 回流して壊れないこと**
- **本番コード中の SQL が実スキーマと合っていること**（`PREPARE` で解析。実行しない）
- **`place_order` の挙動**（残高不足・受付停止・在庫超過・存在しない銘柄）
- **同時注文で売り過ぎないこと**（12 人が最後の 1 杯を同時に取って 1 件だけ通る）
- **同じ券を同時に 8 回読んでも 1 回しか加算されないこと**
- **受渡で在庫が減り、キャンセルでポイントが戻り、二重には動かないこと**

### 環境変数なしで動くこと

CI が `DATABASE_URL` も Clerk の鍵も空にしてビルドを通す。§10 の前提が
崩れていないかを機械的に守るため。

### Neon なしでアプリ全体を動かす

Neon のドライバは Neon の HTTP 口とだけ話すので、ローカルの Postgres には
つながらない。同じ形式を話す中継を立てて `NEON_FETCH_ENDPOINT` を指すと、
Neon のアカウントなしで全体を通せる（`db.ts` 参照）。

### 検証で見つかった実際の不具合

記録として残す。いずれも「実際に動かさないと分からない」種類のものだった。

| 見つかったもの | 原因 | 直し方 |
| --- | --- | --- |
| **開催日が 1 日前になる** | `date` はローカル 0 時の `Date` で返る。`toISOString()` すると日本時間では 9 時間戻る | ローカルの年月日を組み立てる |
| **受付時刻が 9 時間ずれる** | `now.getHours()` はサーバーの時計（UTC）を見る | `+09:00` を付けて絶対時刻に直す |
| **スキーマ適用の排他が無効** | HTTP ドライバは文ごとに接続を閉じるので、セッション版の助言ロックは即解放される | 1 トランザクションにまとめ、`pg_advisory_xact_lock` に変更 |
| **最後の数杯が売り過ぎになる** | CTE は文の開始時点のスナップショットしか見えない | PL/pgSQL 関数に移し、`FOR UPDATE` のあとで数え直す |
| **設定済みなのに未設定と判定** | Vercel の接頭辞つき変数を見ていなかった。空の同名変数もあった | `env.ts` で候補と末尾一致から探す |
| **蔵がログインできない** | メールアドレス必須の設定に対して、username と password だけで作っていた | 受信しないアドレスを割り当てる |
| **蔵が参加者として扱われる** | 役割をセッショントークンから読んでいたが、Clerk は既定で載せない | DB を正として引く |
| **蔵が確認コードで締め出される** | 届かないアドレスに「新しいデバイス」の確認が飛ぶ | 専用画面＋サインイン引換券 |
| **URL を配っても誰も開けない** | Vercel Authentication が既定で有効。自分は開けるので気づけない | 手順書に独立した STEP として明記 |
| **開催日・終了時刻が右枠からはみ出す（iPhone だけ）** | WebKit の `date` / `time` 入力は中身から決まる幅を持ち、`width:100%` では縮まない。`flex` の子の `min-width:auto` と重なって列も縮めなくなる | `appearance:none` と `min-width:0` を当て、列に `min-w-0` を付ける |

---

## 14. 既知の制約・これから

- **お知らせは節目の 4 回だけ。** 券切れ・応答遅延・蔵の受付停止は、ダッシュボードを
  開いたときに初めて分かる。当日「気づくのが遅れた」が起きたら通知の追加を検討する
- **画面を誰も開いていないと、節目のお知らせが送られない。** ポーリングを配信の
  きっかけにしているため。実運用では主催者の画面が開きっぱなしになる前提
- **前売券の QR を外部の販売サイトで発行した場合の受け渡し**が未実装
- **会場マップ**が無い
- **主催者が代理で蔵を操作するとき**、いまは 1 蔵目に固定。蔵を選ぶ UI が必要
- **オフライン対応が無い。** 二重消し込みの危険があるため入れていない。通信が
  切れている間は前回の値を出し続け、警告帯を表示する
- **問い合わせは一問一答。** やり取りが続く用途には向かない
